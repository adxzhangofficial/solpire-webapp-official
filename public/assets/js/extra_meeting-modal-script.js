// Meeting modal interactions are now handled inside the React components
// (see Calendar.tsx and MeetingModal.tsx). This script remains as a stub to
// avoid duplicate event bindings when the legacy HTML bundle still includes it.
console.debug("meeting-modal-script.js: Meeting modal logic is handled via React.")
